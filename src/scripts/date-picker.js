/*
    DATE-PICKER.JS - Last updated: 16-07-15
*/
//-----------------------------------------------------------------
// Variables
//-----------------------------------------------------------------
//-----------------------------------------------------------------
//
//-----------------------------------------------------------------

// jQuery UI on SELECT elements

// if (!Modernizr.inputtypes.date) {
    $(".ui-datepicker").datepicker({ dateFormat: "dd-mm-yy" });
// }

// var datePicker = $('.ui-datepicker');
// var touchDevice = $('.touch').length;
// var isMobile = $(window).width() <= 640;

// if (touchDevice || isMobile) {

// 	datePicker.one('click focus', function(event){
// 		event.preventDefault();
// 		$(this).attr('type', 'date');
// 		$(this).focus();
// 	})

// } else {
// 	$(".ui-datepicker").datepicker({ dateFormat: "dd-mm-yy" });
// }

//-----------------------------------------------------------------
//
//-----------------------------------------------------------------